---
title: "每週一圖作業"
output: html_document
params:
  組員: ["曾雨晴"]  
  學號: ["410773017"]
---

#### 環境設定

以下為老師執行時一定會存在的環境設定
```{r basic setup, message=FALSE, echo=TRUE, eval=T}
library(dplyr); library(stringr); library(ggplot2); library(plotly); library(lubridate); library(readr); library(tidyr); library(showtext)

font_add("QYuan","C:/Users/User/Downloads/cwTeXQYuan-Medium.ttf") # 新增字體
showtext_auto(enable=TRUE) #啟用字體
theme_set(theme_classic())
knitr::opts_chunk$set(out.width='80%', fig.asp=.75, fig.align='center', fig.showtext=T)
```

#### 資料處理

此部份在做：

  * 原始Rda資料引入（即引入**dataList**物件）；
  
  * 資料清理成作圖用graphData物件。

請將你最原始的資料連同作業一起上傳到作業上傳網址：  
  * 若「原始資料為網頁下載」，且下載指令寫在data cleaning chunk裡的話，可以不用上傳「資料」。  
  * 若原始資料有很多個檔案，請先引入成data frame並存在「一個」.Rda檔裡上傳（如`example_data.Rda`檔）。  

資料檔引入到進入作圖前的資料處理請寫在下面區塊，分二部份：

  * 資料處理  
  * 作圖用資料物件生成
  
```{r data cleaning, echo=TRUE, eval=T}
library(readr)
cigaretteRate<- read_csv("https://drive.google.com/uc?export=download&confirm=jeak&id=1JB2eNShp5I4TgF_GMtz_GbKbWiNCpSAU")
cigaretteRate%>%
  slice(c(7:12))%>%
  gather(contains("吸菸率"),
         key="吸菸率類型",value="比率")->cigaretteRate_gather
Year<-cigaretteRate_gather$吸菸率類型%>%
  str_split("年")

cigaretteRate_gather%>%
  mutate(
    年=Year[[ ]][[1]]
  )


cigaretteRate_gather%>%
  mutate(
    教育別=str_sub(分析項目,5,),
    分析項目=as.factor(分析項目),
    吸菸率類型=as.factor(吸菸率類型),
    比率=as.numeric(比率))->cigaretteRate_gather
```

```{r}
library(readr)
loan<- read_csv("https://ws.moe.edu.tw/001/Upload/4/relfile/0/4764/a27e7193-392e-4780-9133-f27d3a65c791.csv", locale = locale(encoding = "BIG5"))

library(readr)
unenployment<- read_csv("https://apiservice.mol.gov.tw/OdService/download/A17000000J-030145-VQ3")

library(readr)
cancer<- read_csv("https://drive.google.com/uc?export=download&confirm=jeak&id=1jiPk0JTDETXBM_Fc7Iwuzs11OIDS12_I", locale = locale(encoding = "BIG5"))

```

#### 繪圖程式

```{r graph, echo=T, eval=T}
cigaretteRate_gather%>%
  ggplot(aes(x=吸菸率類型,y=比率))+
  geom_line()
```

#### 設計說明
說明想傳達的訊息（必要）及設計想法（非必要）。寫在***之後
*** 


